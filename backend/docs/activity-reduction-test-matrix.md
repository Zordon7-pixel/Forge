# Activity-reduction acceptance map

This is a **coverage map, not a release PASS receipt**. Final execution binds an
exact clean SHA and terminal log manifest. No former complete-program release or
intermediate test result substitutes for final activity gates.

## Required scope and remaining policy boundary

Reachable public support: truthful missed outcome; fresh workload/completion
assessment; useful known-distance and duration-only recovery; whole strength
withholding; accept/replay/reload and full-horizon reconciliation.

Public partial strength remains unavailable. The Hermes reachability receipt is
`NEEDS_POLICY` for complete v1. Primitive partial tests below are not substituted
for public partial acceptance. Shipping the narrower requested subset requires
the coordinator's explicit independent design/release scope decision.

## Original reduction-policy adversarial cases

Numbers refer to the original activity-aware policy receipt's 40 cases.

| Cases | Named executable evidence | Boundary |
|---|---|---|
| 1–2 known/duration-only recovery | `activityAdaptationAuthority.smoke.js`; HTTP `recovery`, `duration_only_recovery` | Public generated predecessors, not DB rewritten sources |
| 3–10 original duration/distance/speed/effort/coefficient caps | authority smoke cap loop, numeric pace/registry/coefficient/HR negatives | Original coefficient and physical reference immutable |
| 11–15 identity, independent caps, useful floor, unknown duration | authority context-key and original-binding loops; successor forged owner/session/date/parent; eleven-minute and unknown-duration cases | No other-session headroom or invented duration |
| 16–18 unchanged pools and metadata | authority one/four/seven-pool rest/recovery/reorder matrix; `programDosePolicy.smoke.js` normalization/version/forgery cases | Every unaffected source/vector exact; original inventory preserved |
| 19 partial retained-set primitive | authority partial ledger positive | **Primitive only; public unavailable** |
| 20–30 whole/partial ledger negatives | authority ordinary vs withholding distribution cases; modified allocations/sets/exercise/reps/rest/RPE/load/date/session/duplicate cases; original conservation retained | Whole public HTTP; no set transfer/new exercise/debt |
| 31–34 full program/count/rest | `activityCanonicalSuccessor.smoke.js` real 4/4 and7/7 sources, exact ID inventory, rest-zero, count/occupancy forgery, two successors | Later ordinary weeks unchanged; affected differences disclosed |
| 35–37 locks/goals/window/race and physical/rolling burden | successor parent-goal/session/date forgery and placed-window assertions; authority exact-race/window exclusions; existing goalBackward, canonical, planning-constraint and program normal smokes | Full normal suite is mandatory; no claim that candidate-only checks validate observations |
| 38–39 short easy / missed sick unknown | observation smoke and HTTP `short_easy`; real missed reason-only rejection/readback; completion evidence tests | No two-runs rest rule; sick reason does not fabricate severity |
| 40 unsupported legacy move | actual HTTP default reschedule no-change/bytes checks; `selfServiceRemoval.smoke.js` legacy compatibility | Complete canonical plans never execute legacy move |

## Observed-composition clarification's 50 cases

| Cases | Evidence | Meaning |
|---|---|---|
| 1–10 distinct/duplicate/unknown/zero runs | `activityObservation.smoke.js`; `activityReconciliation.smoke.js`; HTTP `short_easy` and `screenshots` | Two genuine recordings retained; only proven identity deduplicates; no observed family vector |
| 11–16 existing actual protections | observation smoke hard/long/pain/energy controls; `recentRunLoad.smoke.js`; actual HTTP severe-pain, useful recovery, short-easy | Existing physical thresholds/windows unchanged |
| 17–23 future only/nonincrease/full horizon | authority source caps and pool matrix; successor 4/7 full source, every daily/rolling placed vector and distance/duration, two revisions; existing policy/constraint gates | Observed vectors unavailable, not invented; future source authority mandatory |
| 24–29 complete physical lower bound | observation nine-distinct-ID digest and rehashed contradiction negatives; actual manual/null/omitted/linked HTTP; screenshots import/replay/delete | Complete physical digest independent of completion; missing coverage not zero |
| 30–34 immutable history/no headroom | observation context hash; successor parent/evidence forgery; public preview receipt/current reload historical windows; short-easy no-change | Already-incurred workload disclosed, not undone or new capacity |
| 35–40 lift evidence | observation exact watch identity vs distinct manual activities, unknown fields/no vectors; activity completion authority/reconciliation smokes | Explicit/qualified completion separate from physical evidence |
| 41–45 positive acceptance and unsupported transformation | HTTP `recovery`, `duration_only_recovery`, default4/7, `injury_intensity`; reschedule no-change | Whole withholding reachable. Public partial not claimed |
| 46–49 freshness/replay/clock | activity evidence/reconciliation/ticket smokes; `activityPlanVersion` full JSONB-order/real-change negatives; HTTP check-in/new run/correction/delete/import/replay and explicit second accept/replay/settled GET; `programTimezone`, planningRevisionConcurrency and SW normal smokes | Relevant new input invalidates old accept; unchanged settled semantics do not reprompt |
| 50 surface agreement | dated Today/current HTTP status+manifest checks, actual stored canonical IDs; `activityAcceptance.browser.mjs` full5weeks/reload/recovery labels/Brief at320/393; canonical calendar/start normal gates | Browser intercepts persisted real HTTP fixtures; not production or physical phone |

## Integration recipes

Use only the guarded disposable PostgreSQL URL documented by the coordinator.
`activityPersistence.integration.js` creates a unique synthetic database and public
signup/profile/race/preview/apply predecessor. The harness uses a process-confined
advancing September2026 clock. No production clock override exists.

- Default `PROGRAM_TEST_HISTORY=unknown PROGRAM_TEST_ACTIVITY_ACCEPT=1`, repeat
  `PROGRAM_TEST_FREQUENCY=7`: exact missed reason/date/session; null/omitted/owned
  ActiveRun linkage; fresh check-in; keep/no-repeat; stale reject; rest acceptance;
  second accepted revision; prior qualified completion; explicit dated Today.
- `PROGRAM_TEST_ACTIVITY_SCENARIO=recovery PROGRAM_TEST_HISTORY=endurance`:
  independent14mi profile and public four-week history (Sunday5mi, other1.5mi,
  14min/mi). September11 manual5mi/4200s/RPE8. September12 preview retains useful
  recovery from79min protected long→55min and lower-lift whole withholding.
- `duration_only_recovery`, historyunknown: September12 manual1mi/600s/RPE8;
  September13 generated32min duration-only source→22min recovery. Unaffected upper
  lifting remains exact. Opening20min reduced below the existing useful floor is
  correctly rest; this is not rewritten as a positive.
- `short_easy`, historyunknown: two genuine September9 manual1mi/600s/RPE2. No
  automatic rest/proposal from recording count alone.
- `screenshots`, historyunknown: explicitly assumed2026 September8 5.01mi/3239s/HR150
  and September9 3.51mi/2402s/HR149. RPE/pain/energy/zones unknown. Real manual logs,
  deletion, real import and exact source replay retain8.52mi/5641s (canonical meter
  rounding disclosed), no false completion or transport-only repeat prompt.
- `reconciliation`: unlinked work alone is no prescribed completion; exact athlete
  marked completion uses actual fresh workout-start authority; life-event outcome
  persists without moving or completing lift; correction stales old binding;
  replay and accepted-byte preservation.
- `injury_intensity`, historyunknown, repeat `PROGRAM_TEST_RECOVERY=LOW`: public
  moderate shoulder injury. Original RPE7–8 / first-week6–7 cannot be retained under
  injury5–6 intent. Whole ledger persists, no replacement exercise, partial capability
  explicitly unavailable, exact replay and dated reload.

## Evidence discipline

`activityImmutablePrescription.smoke.js` and `activityValidationScope.smoke.js`
cover detached immutable snapshots, unchanged canonical identity, returned-dose
mutation, changed-input revalidation, concurrent/sequential request isolation,
and hostile graph ineligibility. `activityCanonicalSuccessor.smoke.js` compares
scoped/unscoped complete canonical output and rejects different authenticated
owner/context/parent after the same set was already validated. Real HTTP coverage
retains max20 first/second acceptance, replay, stale input and dated reload against
the unchanged 15-second activity endpoint deadline.

All old undated `/plans/today` evidence was withdrawn: an error body is not Today
proof. Current harness requires explicit date, HTTP200, accepted manifest, and exact
execution date. Intercepted browser tests are separately labeled. No account-specific
diagnosis follows from these synthetic fixtures or the supplied screenshots.

Full normal backend/frontend, build, browser/SW and native-contract gates remain
mandatory on the frozen integrated tip. Current policy cases are not all independently
proven merely because a helper smoke passes. Exact Claude review, Hermes release
decision, deploy/live/phone and cleanup verification belong to the coordinator.
