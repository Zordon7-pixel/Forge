You are doing an independent QA review of a backend build on branch feat/adaptive-plan-calendar in this Forge repo. READ and TEST only — do NOT edit or commit anything.

Review the diff: git diff origin/main...HEAD (the new files are backend/src/lib/weeklyRampEngine.js, backend/src/lib/planEffort.js, backend/src/lib/strengthAdjunct.js and their backend/test/*.smoke.js tests). The spec is FORGE-ADAPTIVE-PLAN-CALENDAR-SPEC.md at repo root (Phases 1-3).

Focus areas:
1. weeklyRampEngine.js load math: verify ACWR = 7d load / 28d average daily load with divide-by-zero guards; verify the DELOAD (ACWR>1.5 or red readiness => <=0.9x), HOLD (1.3-1.5 or flat/declining), ADVANCE (<1.3 and stable/improving, capped +10%/wk) branches are correct and mutually consistent; verify the hard 10% cap can never be exceeded in any branch; verify empty/insufficient history returns a passthrough equal to the planned volume (byte-identical to today).
2. planEffort.js: verify no-profile users get byte-identical output to before (no bpm range attached, crude label preserved); verify calibrated users get a targetBpmRange; verify input is not mutated.
3. strengthAdjunct.js: verify full-gym => no substitutions, bodyweight-only => all substituted, partial-gear => correct partial; verify it is fully deterministic (no LLM/random).
4. Confirm NONE of these modules make network/LLM calls and none are wired into plans.js yet (this build is isolated modules only).

Actually RUN the three smoke tests: cd backend && node test/weeklyRampEngine.smoke.js && node test/planEffort.smoke.js && node test/strengthAdjunct.smoke.js — report pass/fail.

Give a verdict line at the very end in exactly this format: VERDICT: PASS  or  VERDICT: NEEDS FIXES — followed by a bullet list of any CRITICAL or HIGH findings (with file:line). Rate each finding CRITICAL / HIGH / MEDIUM. PASS only if zero CRITICAL and zero HIGH.
